name: Custom Workflow

on:
  push:
    branches:
      - main

jobs:
  send_message:
    runs-on: ubuntu-latest

    steps:
      - name: Send POST request
        run: |
          curl -X POST \
            -H "Content-Type: application/json" \
            -d '{"msg": "success"}' \
            http://172.20.10.7:9999
